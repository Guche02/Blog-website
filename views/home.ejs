<%- include("partials/header") -%>

<h1>Home Page</h1>
<p><%= homeContent %></p>

<!-- Didn't work while I treated it as as array and used a for loop. -->
<% posts.forEach(function(post) { %>
    <h1 name="title"><%=post.title %></h1>
    <p name="content"><%=post.content.substring(0,100)%>....<a href= "/posts/<%= post.title %>" >Read More</a></p>
<% }) %>

<%- include("partials/footer") -%>

